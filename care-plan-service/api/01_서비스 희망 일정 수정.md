# 01. 서비스 희망 일정 수정

| 항목     | 내용                                                  |
|--------|-----------------------------------------------------|
| Method | `PATCH`                                             |
| URL    | `/api/v1/service-preferences/{servicePreferenceId}` |
| 사용자    | 퇴원예정자, 사회복지사                                        |
| 카테고리   | 수정                                                  |
| 테이블명   | `p_care_plan_service_preferences`                   |

## 설명

퇴원 예정자가 병원 담당자의 권고 서비스를 수락하여 희망 날짜와 시간대를 입력한다. Care Plan이 아직 확정되기 전 단계에서 사용하는 API다.

## 비즈니스 규칙

- 해당 `servicePreferenceId`가 속한 Care Plan의 `status`가 `UNDER_REVIEW`인 경우에만 수정할 수 있다.
- 본인(퇴원 예정자)의 Care Plan에 속한 희망 일정만 수정할 수 있다.
- 요청자의 `userId`와 `role`은 `X-User-Id`/`X-User-Role` 헤더(Gateway가 인증 후 주입)로 확인한다.

> ⚠️ 확인 필요: Request 명세에 `X-User-Role` 예시값이 `SERVICE_PROVIDER`로 기재되어 있으나, API 설명(퇴원 예정자가 병원 권고 서비스를 수락)과 희망 일정 등록 API(
`ServicePreferenceController`, `hasRole('PATIENT')`)의 기존 구현 기준으로는 `PATIENT`가 맞아 보인다. 원본 스펙 기준 그대로 남겨두었으니 팀 확인 후 정정 필요.
>
> ⚠️ 확인 필요: Method/URL은 원본 스펙에 명시되어 있지 않아, 기존 희망 일정 등록 API(
`POST /api/v1/care-plan-services/{planServiceId}/service-preferences`)와의 리소스 정합성 및 코드 컨벤션(`PUT` 전체수정)을 참고해 추정한 값이다. 팀 확인
> 필요.
>
> ⚠️ 확인 필요: 404 응답 예시의 에러 코드가 `CARE_PLAN_SERVICE_NOT_FOUND`로 되어 있으나, 현재 코드 기준 이 코드는 `CarePlanService`(제공 서비스 선택) 조회 실패에
> 사용 중이며 메시지도 "Care Plan 서비스를 찾을 수 없습니다."이다. `servicePreferenceId` 자체가 존재하지 않는 경우를 나타내려면 별도의
`SERVICE_PREFERENCE_NOT_FOUND` 코드 신설이 필요해 보인다. 원본 스펙 기준 그대로 남겨두었으니 팀 확인 필요.

## Request

| key                 | 설명      | value 타입     | 위치            | 제약사항                   | Nullable | 예시                                     |
|---------------------|---------|--------------|---------------|------------------------|----------|----------------------------------------|
| X-User-Id           | 요청자 Id  | UUID         | Header        | -                      | X        | `4fa85f64-5717-4562-b3fc-2c963f66afa6` |
| X-User-Role         | 요청자 권한  | String(ENUM) | Header        | Gateway가 인증 후 주입, 필수   | X        | `PATIENT`                              |
| servicePreferenceId | 희망일정 ID | UUID         | Path Variable | -                      | X        | `3fa85f64-5717-4562-b3fc-2c963f66afa6` |
| preferredDate       | 희망 날짜   | LocalDate    | Body          | -                      | X        | `2026-09-10`                           |
| preferredTimeSlot   | 희망 시간대  | String(ENUM) | Body          | `MORNING`, `AFTERNOON` | X        | `MORNING`                              |

## Response

| key                 | 설명      | value 타입 | 옵션 | Nullable | 예시                                     |
|---------------------|---------|----------|----|----------|----------------------------------------|
| servicePreferenceId | 희망일정 ID | UUID     | -  | X        | `3fa85f64-5717-4562-b3fc-2c963f66afa6` |

**Example**

```javascript
// 성공
{
    "success"
:
    true,
        "code"
:
    200,
        "message"
:
    "서비스 희망 일정 수정",
        "data"
:
    {
        "servicePreferenceId"
    :
        "3fa85f64-5717-4562-b3fc-2c963f66afa6"
    }
}

// 실패
{
    "success"
:
    false,
        "code"
:
    "CARE_PLAN_SERVICE_NOT_FOUND",
        "message"
:
    "Care Plan 서비스 일정 수정 실패",
        "details"
:
    {
        "reason"
    :
        "servicePreferenceId에 해당 희망일정이 존재하지 않습니다."
    }
,
    "timestamp"
:
    "2026-08-28T03:30:00Z"
}
```

## Status

| status | response content              |
|--------|-------------------------------|
| `200`  | 서비스 희망 일정 수정 성공               |
| `400`  | Validation 실패                 |
| `403`  | 권한 없음                         |
| `404`  | 존재하지 않는 `servicePreferenceId` |

## 변경 이력

| 날짜         | 변경 내용                                                                                                         |
|------------|---------------------------------------------------------------------------------------------------------------|
| 2026-09-04 | 최초 작성 — Notion 원본 스펙 반영. Method/URL, `X-User-Role` 예시값, 404 에러 코드 3건은 원본 스펙과 기존 코드/컨벤션 간 불일치가 있어 ⚠️ 확인 필요로 표시 |
