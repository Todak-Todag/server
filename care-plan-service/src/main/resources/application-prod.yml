spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:5432/todaktodag_db?currentSchema=care_plan_service_schema
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
