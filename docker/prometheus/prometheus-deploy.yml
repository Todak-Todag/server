global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    environment: prod

scrape_configs:
  # Prometheus 자체 상태 (수집 지연, TSDB 크기)
  - job_name: prometheus
    static_configs:
      - targets: ['localhost:9090']
