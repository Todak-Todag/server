# Eureka Server의 로컬 실행 포트를 8761로 지정.
server:
  port: 8761

eureka:
  client:
    # Eureka Server 자기 자신을 서비스로 등록하지 않도록 지정.
    register-with-eureka: false

    # 다른 Eureka Server의 레지스트리를 조회하지 않도록 지정.
    fetch-registry: false