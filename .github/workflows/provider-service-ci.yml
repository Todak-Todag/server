name: provider-service CI

on:
  pull_request:
    branches: [develop, main]
    paths:
      - 'provider-service/**'
      - 'config-repo/provider-service-*.yml'
      - '.github/workflows/provider-service-ci.yml'

permissions:
  contents: read

jobs:
  test:
    name: build & test (Java 21)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: provider-service

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      - uses: gradle/actions/wrapper-validation@v6

      - name: build & test
        run: chmod +x gradlew && ./gradlew build --no-daemon

      - if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: provider-service-test-report
          path: provider-service/build/reports/tests/